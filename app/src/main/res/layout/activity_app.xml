<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent" >
        <android.support.v4.view.ViewPager
                android:id="@+id/mainViewPage"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1.0"
        />
        <RadioGroup
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:id="@+id/mainRadioGroup"
                android:background="@color/color_table_background">
            <RadioButton
                    android:text="首页" style="@style/BottomTabStyle" android:checked="true"
                    android:id="@+id/rb_firstpage"  android:drawableTop="@drawable/selector_firstpage_table"/>
            <RadioButton
                    android:text="订单" style="@style/BottomTabStyle"
                   android:id="@+id/rb_order"  android:drawableTop="@drawable/selector_order_table"/>
            <RadioButton
                    android:text="我的" style="@style/BottomTabStyle"  android:id="@+id/rb_mine"  android:drawableTop="@drawable/selector_mine_table"/>

        </RadioGroup>
    </LinearLayout>
</FrameLayout>
